version: '2'
services:
    php-cli:
        image: lojassimonetti/php-cli-docker:php-7.1
        volumes:
            - ./:/app
        environment:
            - http_proxy=$http_proxy
            - no_proxy=$no_proxy