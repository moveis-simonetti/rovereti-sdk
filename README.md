# rovereti-sdk
Rovereti SDK

## Install dependencies

### Docker

Run on terminal:

```shell
docker-compose up
```
Now, you can run the composer install.

### Composer

```shell
./cli/composer install
```

### Run tests

```shell
./cli/phpunit
```